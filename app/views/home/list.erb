<div class="container">
    <%@every_post.each do |p|%>
    <div class="container">
        
    <hr>
    제목 : <%=p.title%><Br>
    내용 : <%=p.content%><br>
    <hr>
    
    <%p.replies.each do |r|%>
    <p><%=r.comment %></p>
    <%end%>
    
    
      <div class="form-group">
        <label>댓글</label>
        <input type="hidden" name="post_id" value="<%=p.id%>" id="post_id_<%=p.id%>">
        <input type="text"  name="comment" class="form-control" id="comment_<%=p.id%>" placeholder="댓글">
      </div>

      <button type="submit" class="btn btn-default">Submit</button>
  
    
    <hr>
    </div>
    <%end%>
    
    
    
</div>

<a href="/home/index">홈으로</a>

<script>
  $.ajax({
  method: "POST",
  url: "some.php",
  data: { name: "John", location: "Boston" }
  })
  .done(function( msg ) {
    alert( "Data Saved: " + msg );
  });
  
</script>